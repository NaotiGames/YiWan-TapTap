<?xml version="1.0" encoding="utf-8"?>
<root xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- init section is always evaluated once per architecture -->
    <init>
        <log text="LeanCloudMobile Android Build init"/>
    </init>

    <!-- optional additions to proguard -->
    <proguardAdditions>
        <insert>
            # proguard.cfg
            -keepattributes Signature
            -dontwarn com.jcraft.jzlib.**
            -keep class com.jcraft.jzlib.**  { *;}
            -dontwarn sun.misc.**
            -keep class sun.misc.** { *;}
            -dontwarn retrofit2.**
            -keep class retrofit2.** { *;}
            -dontwarn io.reactivex.**
            -keep class io.reactivex.** { *;}
            -dontwarn sun.security.**
            -keep class sun.security.** { *; }
            -dontwarn com.google.**
            -keep class com.google.** { *;}
            -dontwarn com.tapsdk.lc.**
            -keep class com.tapsdk.lc.** { *;}
            -keep public class android.net.http.SslError
            -keep public class android.webkit.WebViewClient
            -dontwarn android.webkit.WebView
            -dontwarn android.net.http.SslError
            -dontwarn android.webkit.WebViewClient
            -dontwarn android.support.**
            -dontwarn org.apache.**
            -keep class org.apache.** { *;}
            -dontwarn okhttp3.**
            -keep class okhttp3.** { *;}
            -keep interface okhttp3.** { *; }
            -dontwarn okio.**
            -keep class okio.** { *;}
            -keep class com.tapsdk.lc.** { *;}
            -keepattributes *Annotation*
        </insert>
    </proguardAdditions>

    <androidManifestUpdates>
        <addPermission android:name="android.permission.INTERNET"/>
        <addPermission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    </androidManifestUpdates>

    <resourceCopies>
        <copyDir src="$S(PluginDir)/Private/Android/Java/" dst="$S(BuildDir)/src/com/tapsdk/lc/" />
    </resourceCopies>

    <buildGradleAdditions>
        <insert>
            dependencies.implementation('com.taptap:lc-realtime-android:8.2.24')
            dependencies.implementation('com.taptap:lc-storage-android:8.2.24')
            dependencies.implementation('io.reactivex.rxjava2:rxandroid:2.1.1')
        </insert>
    </buildGradleAdditions>
</root>
